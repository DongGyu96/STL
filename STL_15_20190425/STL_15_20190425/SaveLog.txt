

==================
Thu Apr 25 13:34:46 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;

int main()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		if (isalpha(*i))
			alphaNum[tolower(*i)]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;

	save("main.cpp");
}

==================
Thu Apr 25 13:37:41 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;

void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("Alice_in_Wonderland.pdf");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		if (isalpha(*i))
			alphaNum[tolower(*i)]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:38:08 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;

void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("Alice_in_Wonderland.pdf");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		if (isalpha(*i))
			alphaNum[tolower(*i)]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:40:52 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:41:10 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:43:04 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("Carroll__Alice_1st.pdf");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:43:08 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("Carroll__Alice_1st.pdf");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:44:38 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:46:07 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:46:33 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	int count = 0
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
		count++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;

	cout << count << endl;
}

==================
Thu Apr 25 13:46:47 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	int count = 0;
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
		count++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;

	cout << count << endl;
}

==================
Thu Apr 25 13:48:38 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<string, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	CountNum();


	save("main.cpp");
}

void CountNum()
{
	int count = 0;
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		alphaNum[*i]++;
		count++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;

	cout << count << endl;
}

==================
Thu Apr 25 13:52:48 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
void CountNum(); // 문자열 빈도수 측정

int main()
{
	map<string, int> words;
	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}
	for (auto i = words.begin(); i != words.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:57:28 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const auto& d : words)
	{
		count[d.second] = d.first;
	}

	for (auto i = count.begin(); i != count.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:58:01 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const auto& d : words)
	{
		count[d.second] = d.first;
	}

	for (auto i = count.begin(); i != count.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << count.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 13:58:09 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const auto& d : words)
	{
		count[d.second] = d.first;
	}

	for (auto i = count.begin(); i != count.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:02:18 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.first, d.second);
	}

	for (auto i = count.begin(); i != count.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:02:36 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (auto i = count.begin(); i != count.end(); ++i)
		cout << i->first << " --> " << i->second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:03:30 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (const auto& d: count)
		cout << d.first << " --> " << d.second << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:03:44 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (const auto& d: count)
		cout << d.second << " --> " << d.first << endl;

	cout << "전체 단어 수 " << words.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:05:02 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	map<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (const auto& d: count)
		cout << d.second << " --> " << d.first << endl;

	cout << "전체 단어 수 " << words.size() << endl;
	cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:05:26 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;
	set<int, string> count;

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (const auto& d: count)
		cout << d.second << " --> " << d.first << endl;

	cout << "전체 단어 수 " << words.size() << endl;
	cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:06:09 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	multimap<int, string> count; // 동일 키값 중복 허용

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	for (const pair<string, int>& d : words)
	{
		count.emplace(d.second, d.first);
	}

	for (const auto& d: count)
		cout << d.second << " --> " << d.first << endl;

	cout << "전체 단어 수 " << words.size() << endl;
	cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:11:46 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:12:07 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 맵의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:12:18 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:12:26 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 5000; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:12:34 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("test.txt");
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:14:01 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream in("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:24:09 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	for (int i = 0; i< word.size(); ++i)
	{
		if (isalpha(word[i]))
			word[i] = " ";
	}
	ostream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, "\n"));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:25:01 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	for (int i = 0; i< word.size(); ++i)
	{
		if (isalpha(word[i]))
			word[i] = " ";
	}
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, "\n"));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:28:39 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, " ");
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, "\n"));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:28:55 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, " ");
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, "\n"));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:29:04 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, "\n"));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:29:28 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:29:45 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out, " "));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:29:50 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istream_iterator<char>(inOld), istream_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:29:56 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:30:08 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostream_iterator<char>(out));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:30:16 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));
	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:33:32 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:33:37 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:33:45 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다

	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	replace_if(word.begin(), word.end(), [](char c) {
		return !isalpha(c);
	}, ' ');
	ofstream out("New_Alice.txt");
	copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	ifstream inNew("New_Alice.txt");

	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 4314; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:36:29 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](char c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:36:42 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](char c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:37:10 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](char& c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:37:18 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:37:43 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](char& c) {
		if (!isalpha(c)) c = ' ';
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:37:53 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](char& c) {
		if (!isalpha(c)) 
			c = ' ';
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:38:13 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:38:29 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:39:04 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		else return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:40:13 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:40:27 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostreambuf_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:40:39 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c)) return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:41:46 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:42:15 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(word.begin(), word.end(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:45:57 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };

	// 두가지 방법	replace_if, transform
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라



	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 14:46:09 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	}

	vector<pair<string, int>> v{ words.begin(), words.end() };

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	});

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라



	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:04:58 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 문자와 사용횟수를 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<char, int> chars;

	for (auto i = istreambuf_iterator<char>(in); i != istreambuf_iterator<char>(); ++i)
	{
		chars[*i]++;
	}

	for (const pair<char, int>& d : chars)
		cout << d.first << " --> " << d.second;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:05:07 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 문자와 사용횟수를 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<char, int> chars;

	for (auto i = istreambuf_iterator<char>(in); i != istreambuf_iterator<char>(); ++i)
	{
		chars[*i]++;
	}

	for (const pair<char, int>& d : chars)
		cout << d.first << " --> " << d.second << endl;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:06:01 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 문자와 사용횟수를 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<char, int> chars;

	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		chars[*i]++;
	}

	for (const pair<char, int>& d : chars)
		cout << d.first << " --> " << d.second << endl;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:08:08 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		chars[*i]++;
	}

	for (const pair<string, int>& d : chars)
		cout << d.first << " --> " << d.second << endl;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:08:50 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
	{
		chars[*i]++;
	}

	for (const pair<string, int>& d : chars)
		cout << d.first << " --> " << d.second << endl;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:09:08 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
		chars.emplace(*i, *i);

	for (const pair<string, int>& d : chars)
		cout << d.first << " --> " << d.second << endl;


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:10:45 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
		chars.emplace(*i, *i);

	// 정렬된 단어를 파일에 기록하자
	ofstream fout("단어 오름차순 정렬한 앨리스.txt");

	for (const pair<string, int>& d : chars)
		out << d.first << endl;

	fout.close();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:11:29 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
		chars.emplace(*i, *i);

	// 정렬된 단어를 파일에 기록하자
	ofstream fout("words.txt");

	for (const pair<string, int>& d : chars)
		out << d.first << endl;

	fout.close();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:12:05 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
		chars.emplace(*i, *i);

	// 정렬된 단어를 파일에 기록하자
	ofstream fout("단어 오름차순 정렬한 앨리스.txt");

	for (const pair<string, int>& d : chars)
		fout << d.first << endl;

	fout.close();


	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}

==================
Thu Apr 25 15:13:13 2019

==================
// ====================================================
// 연관 컨테이너 ( 원소를 빨리 찾기 위한 컨테이너 )
//
//
// ====================================================
#include <iostream>
#include <map>
#include <fstream>
#include <string>
#include <iterator>
#include <vector>
#include <algorithm>
#include "save.h"
#include "String.h"
using namespace std;

map<char, int> alphaNum;
void CountNum(); // 문자 빈도수 측정

// "이상한 나라의 앨리스"에 사용된 단어와 사용횟수를 출력하라
// 사용된 단어를 사용 횟수 오름차순으로 출력하라

int main()
{
	map<string, int> words;

	// multimap<int, string> count; // 동일 키값 중복 허용
	// 그러나 vector를 쓰는게 훨씬 빠르다 ( 집어넣을때마다 정렬을 한다 )
	// 벡터에 한번에 넣은 후 정렬이 효율적
	/*for (const pair<string, int>& d : words)
	{
	count.emplace(d.second, d.first);

	}
	for (const auto& d: count)
	cout << d.second << " --> " << d.first << endl;*/

	ifstream inOld("alice.txt"); // 그러나 앨리스라는 책에는 기호가 많이 사용됨
	// replaceif 라는 것을 사용한다면 ( 알파벳으로 이루어진 단어만 처리하도록 입력 파일 가공 )
	// 입력 파일에서 알파벳이 아닌 문자는 공백으로 대체한다
	ofstream out("New_Alice.txt");

	// 두가지 방법	replace_if, transform
	//vector<char> word{ istreambuf_iterator<char>(inOld), istreambuf_iterator<char>() };
	//replace_if(word.begin(), word.end(), [](char c) {
	//	return !isalpha(c);
	//}, ' ');
	//copy(word.begin(), word.end(), ostreambuf_iterator<char>(out));

	//transform(입력시작, 입력끝, 목적지, 변신방법);
	transform(istreambuf_iterator<char>(inOld), istreambuf_iterator<char>(), ostream_iterator<char>(out), [](const char& c) {
		if (!isalpha(c) && c != '\n') return ' ';
		return c;
	});

	inOld.close();
	out.close();
	ifstream inNew("New_Alice.txt");
	for (auto i = istream_iterator<string>(inNew); i != istream_iterator<string>(); ++i)
	{
		words[*i]++;
	} // 단어 횟수 체크

	vector<pair<string, int>> v{ words.begin(), words.end() }; // 처음부터 끝까지 삽입

	sort(v.begin(), v.end(), [](const pair<string, int>& a, const pair<string, int>& b){
		return a.second > b.second;
	}); // 정렬

	for (int i = 0; i < 20; ++i)
	{
		cout << v[i].first << " --> " << v[i].second << endl;
	}

	cout << "전체 단어 수 " << words.size() << endl;
	// cout << "횟수로 정렬한 맵의 원소 수 " << count.size() << endl;
	cout << "횟수로 정렬한 벡터의 원소 수 " << v.size() << endl << endl;

	inNew.close();



	// "알파벳만 남긴 앨리스.txt"에 사용된 단어와 횟수를 알고 있다.
	// 사용자가 입력한 단어가 몇번 사용되었는가 출력하라
	while (0)
	{
		string input;
		cout << "검색하려는 단어 : ";
		cin >> input;

		//auto p = find_if(words.begin(), words.end(), [&input](const pair<string, int>& d) {
		//	return input == d.first;
		//});
		auto p = words.find(input);
		if (p != words.end())
			cout << input << "의 사용 횟수 : " << p->second << endl;
		else
			cout << input << "는 앨리스에 없는 단어입니다" << endl;

		// cout << "검색하려는 단어 : " << input << " --> " << words[input] << endl;
	}

	// "알파벳만 남긴 앨리스.txt"에 사용된 단어를 오름차순으로 컨테이너에 저장하자
	ifstream in ("New_Alice.txt");
	map<string, int> chars;

	for (auto i = istream_iterator<string>(in); i != istream_iterator<string>(); ++i)
		chars.emplace(*i, 1);

	// 정렬된 단어를 파일에 기록하자
	ofstream fout("단어 오름차순 정렬한 앨리스.txt");

	for (const pair<string, int>& d : chars)
		fout << d.first << endl;

	in.close();
	fout.close();
	// 단어가 정렬되어 있다 a로 시작하는 단어는 몇개인가?

	save("main.cpp");
}

void CountNum()
{
	ifstream in("main.cpp");
	for (auto i = istream_iterator<char>(in); i != istream_iterator<char>(); ++i)
	{
		alphaNum[*i]++;
	}
	for (const auto& d : alphaNum)
		cout << "[" << d.first << "] --> " << d.second << endl;
}